using System;
using System.Collections.Generic;
using System.Text;

namespace Minecraft_Texture_Load_and_Basic_Movement
{
    class FPS_Counter
    {

        /*
        SpriteBatch spriteBatch = new SpriteBatch(GraphicsDevice);
        spriteBatch.Begin();
        spriteBatch.DrawString(spriteFont, "Hello", new Vector2(0, 0), Color.White);
        spriteBatch.End();
        */
    }
}
